# Backend Service Configuration Example
# Copy this file to .env and fill in your own values
# DO NOT commit the .env file - it contains sensitive credentials

# ============================================================================
# OAuth Configuration (Better Auth)
# ============================================================================

# CRITICAL: This must match APP_BASE_URL in the router
# Better Auth will redirect OAuth callbacks to: {APP_BASE_URL}/auth/callback/{provider}
# In production: https://router.artorizer.com
# In development: http://localhost:7000
APP_BASE_URL="http://localhost:7000"

# ============================================================================
# OAuth Providers - Get credentials from:
# Google: https://console.cloud.google.com/
# GitHub: https://github.com/settings/developers
# ============================================================================

# GitHub OAuth
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
GOOGLE_PROJECT_ID="your-google-project-id"
GOOGLE_AUTH_URI="https://accounts.google.com/o/oauth2/auth"
GOOGLE_TOKEN_URI="https://oauth2.googleapis.com/token"
GOOGLE_AUTH_PROVIDER_CERT_URL="https://www.googleapis.com/oauth2/v1/certs"
GOOGLE_REDIRECT_URI="http://localhost"

# ============================================================================
# Better Auth & Encryption Secrets
# CRITICAL: Generate new secrets for production
# Generate with: openssl rand -hex 32
# ============================================================================

# Better Auth signing key - used to sign session tokens
BETTER_AUTH_SECRET="your-better-auth-secret-here"

# AES-256-GCM key for PII encryption (email, username, name)
# Base64-encoded 32-byte key
# Generate with: openssl rand -base64 32
APP_ENCRYPTION_KEY="your-app-encryption-key-here"

# ============================================================================
# Internal Service Authentication
# CRITICAL: Must match INTERNAL_API_KEY in the router's .env
# The router uses this key to authenticate when calling /tokens endpoint
# Generate with: openssl rand -hex 32
# ============================================================================

INTERNAL_API_KEY="your-internal-api-key-here"
